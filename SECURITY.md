# 安全说明

pi-roleplay 面向单用户自托管，不提供租户隔离。模型 Bash 在工具容器中执行，同一部署中的会话共享此容器；工作区权限限制可用操作，但不构成对恶意用户的安全边界。

生产服务只通过 Caddy 暴露 HTTPS，工具服务不发布公网端口。调试服务只绑定本机地址。不要向工具容器添加应用数据、模型密钥、宿主机目录或 Docker socket 挂载。

如果发现安全问题，请使用仓库托管平台提供的私密漏洞报告功能，或维护者公开提供的私密联系方式。仓库尚未启用私密报告渠道时，不要在公开 Issue 中附上有效凭证、私人会话、可利用的部署地址或完整利用细节。

备份中的故事、附件与工作文件都是私有数据。管理员密码重置会撤销原登录，但不会替换模型 API 密钥；如果密钥曾经公开，应在提供方撤销并重新配置。仅删除文件或加入 `.gitignore` 不会清除 Git 历史。
