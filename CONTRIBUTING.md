# 参与开发

先阅读 [架构说明](docs/architecture.md) 与 [本地开发](docs/development.md)，使用独立的调试数据。新功能和修复保持 TypeScript、清晰的模块职责，以及对应的行为测试。

提交前运行 `pnpm verify`、`pnpm test:backup` 和 `pnpm release:check`。修改容器挂载、凭证、数据库迁移或部署流程时，还应运行相关容器与恢复验收。

问题报告应说明触发步骤、预期行为、实际结果和版本。日志、截图、数据库与角色卡可能包含私人内容，先删除密钥、个人信息和未经授权的资料；优先使用原创合成案例复现。请不要把运行目录、模型密钥或真实会话作为测试夹具提交。

界面默认中文并提供 English 文案。正文、角色资料与用户 Prompt 保持原文，不随界面语言改写。每轮剧情事实以故事事件为准，失败和部分成功必须明确显示。

贡献代码须有权按项目 MIT 许可提供；迁入或参考的代码保留原作者、许可证与来源记录。新增依赖和图片需要更新第三方说明及公开检查规则。
